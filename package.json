{
  "name": "ats-platform",
  "version": "1.0.0",
  "description": "ATS Platform - Applicant Tracking System",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
